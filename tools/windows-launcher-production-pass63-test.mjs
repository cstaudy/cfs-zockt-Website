import fs from 'node:fs';import path from 'node:path';import process from 'node:process';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const root=path.resolve(process.argv[2]||'.'),mod=require(path.join(root,'launcher/src/windows-production-drill.js'));const src=fs.readFileSync(path.join(root,'launcher/tools/windows-production-drill-r63.mjs'),'utf8');let p=0,f=0;const t=(n,o)=>{console.log(`${o?'PASS':'FAIL'} ${n}`);o?p++:f++};const state={release_version:'0.42.0',platform:'win32',arch:'x64',windows_build:22631,artifact_sha256:'a'.repeat(64),signature_status:'Valid',expected_signer_thumbprint:'B'.repeat(40),actual_signer_thumbprint:'B'.repeat(40),clean_install_verified:true,launcher_started_verified:true,device_link_verified:true,updater_verified:true,audio_hardware_verified:true,game_capture_hardware_verified:true};
t('R63 evaluator accepts complete evidence',mod.evaluateDrill(state).ok);t('R63 rejects unsigned artifact',!mod.evaluateDrill({...state,signature_status:'NotSigned'}).ok);t('R63 rejects signer mismatch',!mod.evaluateDrill({...state,actual_signer_thumbprint:'C'.repeat(40)}).ok);t('R63 uses Windows Authenticode',src.includes('Get-AuthenticodeSignature'));t('R63 runs audio hardware doctor',src.includes('audio-helper:doctor'));t('R63 runs game capture hardware doctor',src.includes('game-capture:doctor'));t('R63 requires real clean install/updater/device link evidence',src.includes('CFS_WINDOWS_CLEAN_INSTALL_VERIFIED')&&src.includes('CFS_WINDOWS_UPDATER_VERIFIED')&&src.includes('CFS_WINDOWS_DEVICE_LINK_VERIFIED'));
console.log(`\nWindows Launcher Production Security R63: ${p}/${p+f} PASS`);if(f)process.exit(1);
