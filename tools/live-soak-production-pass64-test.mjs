import path from 'node:path';import process from 'node:process';import {createRequire} from 'node:module';const require=createRequire(import.meta.url),root=path.resolve(process.argv[2]||'.'),d=require(path.join(root,'launcher/src/live-soak-production-drill.js'));let p=0,f=0;const t=(n,o)=>{console.log(`${o?'PASS':'FAIL'} ${n}`);o?p++:f++};const start=Date.parse('2026-01-01T00:00:00Z'),beats=Array.from({length:121},(_,i)=>new Date(start+i*60000).toISOString()),state={release_version:'0.42.0',platform:'win32',windows_build:22631,started_at:beats[0],ended_at:beats.at(-1),heartbeats:beats,segments:[{started_at:beats[0],ended_at:beats.at(-1)}],fatal_errors:0,obs_connected_final:true,live_provider_connected_final:true,user_confirmed_no_visible_failures:true};const ev={status:'LIVE_SOAK_PASS',verified_at:new Date().toISOString(),state_sha256:d.stateSha256(state),state};t('R64 requires at least two hours',d.evaluateState(state).ok);t('R64 rejects short soak',!d.evaluateState({...state,ended_at:new Date(start+60*60000).toISOString(),heartbeats:beats.slice(0,61)}).ok);t('R64 rejects fatal error',!d.evaluateState({...state,fatal_errors:1}).ok);t('R64 rejects missing OBS confirmation',!d.evaluateState({...state,obs_connected_final:false}).ok);t('R64 final evidence validates hash and state',d.validateFinalEvidence(ev).ok);t('R64 rejects evidence tampering',!d.validateFinalEvidence({...ev,state:{...state,fatal_errors:1}}).ok);console.log(`\nLIVE Soak Production Security R64: ${p}/${p+f} PASS`);if(f)process.exit(1);
